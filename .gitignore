# Monorepo root — see server/.gitignore and ios/.gitignore for subproject rules

# OS / editor
.DS_Store

# Local tooling & agent scratch (not part of the product)
.claude/*
# ...but shared agent skills are checked in
!.claude/skills/
.superpowers/
.deepsec/
.clawpatch/
.agents/
skills-lock.json

# Ad-hoc reports
report.md

# Python coverage (root-level stray)
.coverage
